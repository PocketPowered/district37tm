query EventDetails($id: BigInt!) {
  eventsCollection(first: 1, filter: { id: { eq: $id } }) {
    edges {
      node {
        id
        images
        title
        description
        start_time
        end_time
        location_info
        additional_links
        date_key
        tag
      }
    }
  }
}
