query AllLocations($conferenceId: BigInt!) {
  locationsCollection(
    filter: { conference_id: { eq: $conferenceId } }
    orderBy: [{ location_name: AscNullsLast }]
  ) {
    edges {
      node {
        id
        location_name
        location_images
      }
    }
  }
}
