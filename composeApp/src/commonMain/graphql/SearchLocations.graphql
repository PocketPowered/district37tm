query SearchLocations($nameLike: String!) {
  locationsCollection(
    filter: { location_name: { ilike: $nameLike } }
    orderBy: [{ location_name: AscNullsLast }]
  ) {
    edges {
      node {
        id
        location_name
        location_images
      }
    }
  }
}

