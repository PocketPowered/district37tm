mutation VerifyPhoneOtp($input: VerifyPhoneOtpInput!) {
    verifyPhoneOtp(input: $input) {
        accessToken
        refreshToken
        expiresIn
        isNewUser
        user {
            id
            email
            username
            displayName
            bio
            profileImageUrl
        }
    }
}
