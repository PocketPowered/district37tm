mutation MarkConversationAsRead($input: MarkAsReadInput!) {
    markConversationAsRead(input: $input) {
        conversationId
        userId
        joinedAt
        lastReadAt
        lastReadMessageId
    }
}
