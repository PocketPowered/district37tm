#import "./ImageDetails.graphql"

fragment LocationDetails on Location {
  id
  slug
  name
  venueId
  locationType
  capacity
  floorLevel
  description
  createdAt
  imagesConnection(first: 10) {
    edges {
      node {
        ...ImageDetails
      }
    }
  }
}
