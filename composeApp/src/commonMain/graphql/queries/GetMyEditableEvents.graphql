#import "../fragments/EventPreview.graphql"
#import "../fragments/PaginationInfo.graphql"

query GetMyEditableEvents($first: Int, $after: String) {
    myEditableEventsConnection(first: $first, after: $after) {
        edges {
            node {
                ...EventPreview
            }
        }
        pageInfo {
            ...PaginationInfo
        }
        totalCount
    }
}
