#import "../fragments/EventPreview.graphql"
#import "../fragments/PaginationInfo.graphql"

query GetMoreOrganizationEvents($id: Int!, $first: Int, $after: String) {
  organization(id: $id) {
    id
    eventsConnection(first: $first, after: $after) {
      edges {
        node {
          ...EventPreview
        }
      }
      pageInfo {
        ...PaginationInfo
      }
    }
  }
}
