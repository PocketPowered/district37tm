query GetMyFriends($first: Int, $after: String) {
    myFriends(first: $first, after: $after) {
        edges {
            cursor
            node {
                userId
                user {
                    ...UserPreview
                    username
                }
            }
        }
        pageInfo {
            ...PaginationInfo
        }
        totalCount
    }
}
